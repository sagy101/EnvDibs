export const MAX_TTL_SECONDS = 3 * 24 * 60 * 60; // 3 days
export const REMINDER_LEAD_SECONDS = 10 * 60; // 10 minutes before expiry
export const REMINDER_MIN_TTL_SECONDS = 30 * 60; // send reminders only if original TTL >= 30m
